<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TPQuickPlaceByKakaoSearchAPI" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brand</item>
        <item name="colorPrimaryVariant">@color/brand</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="fontFamily">@font/binggrae</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- splash 맥티비티 스타일 테마   -->
    <style name="SplashTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
<!--        <item name="android:windowBackground">@color/black</item>-->
        <!--  여러장의 이미지를 겹쳐서 하나의 그림으로 만드는 layer-list 로 만든 스플래시 이미지 배경 적용   -->
        <item name="android:windowBackground">@drawable/splash</item>

        <!--  상태표시줄이 보이지 않도록..      -->
        <item name="android:windowFullscreen">true</item>
    </style>


</resources>